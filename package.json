{
  "name": "nielseniqebit",
  "module": "index.ts",
  "dependencies": {
    "browser-sync": "^2.29.3"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "browser-sync start --server --files 'app/*.css' 'app/*.js' '*.html' "
  },
  "type": "module"
}